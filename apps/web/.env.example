# 切换 mock / real 后端。
# 未设置、空值、"true"、"1" 都视为 mock（本地开发无后端时的默认行为）。
# 设为 "false" 或 "0" 切换到 real 模式。
NEXT_PUBLIC_USE_MOCK=true

# real 模式下的 API base URL，不要带尾部斜杠。
# 示例：NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_API_BASE_URL=