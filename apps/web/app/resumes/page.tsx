import { ResumeParseWorkbench } from "@/components/resume-parse-workbench";

export const metadata = { title: "简历解析确认 · Hiro2" };

export default function ResumesPage() {
  return <ResumeParseWorkbench />;
}
