## 基本信息
- 姓名：李同学
- 电话：138****0000
- 邮箱：litongxue@example.com
- 求职意向：大数据开发工程师（Spark/Flink/数仓方向）

## 技能
- **离线计算**：Spark（PySpark 为主）做ETL和批处理，搞过动态分区、广播变量优化，踩过数据倾斜的坑（加盐+两阶段聚合解决）。
- **实时计算**：Flink（1.17 版本）搞过事件时间+水位线处理，用过 checkpoint 对齐，玩过状态后端切换（RocksDB）。
- **数仓建模**：熟悉星型/雪花模型设计，用过 Hive/Spark SQL 调优（CBO、小文件合并、谓词下推），做过拉链表和缓慢变化维（SCD2）。
- **调度与存储**：Airflow 调度（2.5+）搭过DAG依赖，配过重试和告警；Kafka（2.8 起）消费端手动提交offset，处理过rebalance问题。
- **数据湖**：Hudi（0.13）做过COW表和MOR表，解决过小文件问题的 filegroup 合并。

## 项目经历
**项目：用户行为实时分析平台（2023.06 - 2024.02）**
- 用 Flink（1.17 版本）消费 Kafka（2.8 起）的用户点击流，窗口聚合（滚动+会话）产出实时PV/UV，写入 Hudi（0.13）表供下游看板查询。
- 处理过背压问题（增加并行度+调整 buffer.memory），用 RocksDB 做状态后恢复，保证 exactly-once。
- 离线部分用 Spark（PySpark 为主）每天跑全量用户画像，关联日志表做特征宽表，配合 Hive/Spark SQL 调优把跑批时间从 2 小时压到 40 分钟。

**项目：数仓核心层重构（2022.09 - 2023.05）**
- 负责 DWD 层事实表清洗，处理脏数据（JSON解析异常、字段缺失），用 Airflow 调度（2.5+）搭 ODS->DWD->DWS 的依赖链，加失败重跑和超时告警。
- 设计用户维度拉链表，处理历史变更追踪，用 Hudi（0.13）的 upsert 能力解决更新覆盖问题。

## 教育背景
- 某理工大学，计算机科学与技术，本科（2018 - 2022）

## 证书
- 无（但平时爱看 Flink 官方文档和社区博客，也刷过一些性能调优案例）
