# ADR 0001：环境管理

## 背景

项目同时包含 Python 数据/AI 服务和 TypeScript 前端，需要可复现的本地与 CI 环境。

## 决定

- Python 依赖、虚拟环境和锁文件使用 `uv`。
- TypeScript/Node 依赖和锁文件使用 `pnpm`。
- 不使用 `pip`、`npm`、`yarn` 或全局项目依赖。

## 后果

所有成员通过 `make init` 初始化环境；CI 使用锁文件安装依赖。

## 替代方案

不采用 Poetry、Conda、npm 或 Yarn，避免同一语言存在多套环境入口。
