# ADR 0002：事实与图谱存储

## 背景

岗位版本、审核、评测和证据需要事务与版本记录；图谱需要多跳查询与可视化。

## 决定

- PostgreSQL 保存业务事实、审核、评测和运行记录。
- Neo4j 只保存已发布岗位版本的图谱投影。
- 向量索引、Neo4j 和缓存均可从事实主库重建。

## 后果

Neo4j 不可用不会阻塞岗位版本发布或人岗匹配。

## 替代方案

不以 Neo4j 作为唯一主库，避免图谱同步和版本审计风险。
